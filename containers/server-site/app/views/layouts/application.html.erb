<!DOCTYPE html>
<html>
  <head>
    <title>Minecraft Server Dashboard</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Styles are inline in the dashboard view, so no external stylesheet needed %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
